<% content_for(:page_title) {t '.page_title', :pick => @pick.title } %>
<% content_for(:title) { } %>
<% content_for(:admin_menu) {navbar_link_to t('buttons.edit'), edit_pick_path(@pick), {:class => :edit, :title => t('buttons.edit')}} %>

<div class="row">
  <%= render @pick %>
</div>

<div class="row">
  <p><%= t 'data_about_thing_as' %> .json and .geojson</p>
</div>