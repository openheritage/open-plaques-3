<% content_for(:page_title) {t '.page_title', :role => @pluralized_role } %>
<% content_for(:title) { } %>
<% content_for(:admin_menu) {navbar_link_to t('buttons.edit'), edit_role_path(@role), {:class => :edit, :title => t('buttons.edit')}} %>

<div class="row">
  <%= render @role %>
</div>

<div class="container-fluid">
  <div class="card-columns">
    <%= render @role.personal_roles %>
  </div>
</div>

<% if @role.related_roles.size > 0 %>
  <div class="row">
    <h2><%= t '.related_roles'%></h2>
  </div>

  <% for related_role in @role.related_roles %>
    <div class="row">
      <%= render related_role %>
    </div>
    
    <div class="container-fluid">
      <div class="card-columns">
        <%= render related_role.personal_roles %>
      </div>
    </div>
  <% end %>
<% end %>

<div class="row">
  <p><%= t 'data_about_thing_as' %> .json and .geojson</p>
</div>