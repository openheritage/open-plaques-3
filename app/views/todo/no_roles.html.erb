<% content_for :page_title, "To do: people with no role" %>

<div class="panel">
  <h1>Subjects with no role</h1>
  <section class="admin_panel"><%= link_to("All to-do lists", "/todo") %></section>
  <% if @people.size > 0 %>
    <%= will_paginate @people %>
    <%= render @people %>
    <%= will_paginate @people %>
  <% else %>
    <p>Every person has a role. Bravo!</p>
  <% end %>
</div>