<% content_for :page_title, "To do: plaques with partial inscriptions" %>
<% content_for :title, "Plaques with partial inscriptions" %>
<% content_for(:admin_menu) {navbar_link_to("All to-do lists", "/todo")} %>

<% if @plaques.size > 0 %>
  <div class="container-fluid">
    <%= will_paginate @plaques %>
    <div class="card-columns">
      <%= render @plaques %>
    </div>
    <%= will_paginate @plaques %>
  </div>
<% else %>
  <p>Every plaque has an inscription. Bravo!</p>
<% end %>