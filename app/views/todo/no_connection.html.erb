<% content_for :page_title, "To do: UK plaques with no connections" %>
<% content_for :title, "UK plaques with no connections" %>
<% content_for :page_subhead, "#{@no_connection_count} out of #{@uk_plaques_count} == #{@no_connection_percentage}%" %>
<% content_for(:admin_menu) {navbar_link_to("All to-do lists", "/todo")} %>

<% if @plaques.size > 0 %>
  <%= will_paginate @plaques %>
  <div class="card-columns">
    <%= render @plaques %>
  </div>
  <%= will_paginate @plaques %>
<% else %>
  <p>Every plaque has connections. Bravo!</p>
<% end %>
