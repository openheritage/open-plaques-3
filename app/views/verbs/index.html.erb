<% content_for(:page_title) {t '.page_title' } %>
<% content_for(:title) {t '.page_heading' } %>
<% content_for(:admin_menu) {navbar_link_to t('buttons.add'), new_verb_path, {:class => :edit, :title => t('buttons.add')}} %>

<div class="container-fluid">
  <div class="card-columns">
    <%= render @verbs %>
  </div>
</div>

<div class="row">
  <p><%= t 'data_about_things_as' %> .json</p>
</div>