<% if content_for?(:title) || content_for?(:page_subhead) %>
  <div class="jumbotron">
    <% if content_for?(:title) %>
      <h1 property="dc:title rdfs:label foaf:name" about="#this"><%= content_for(:title) %></h1>
    <% end %>
    <%= content_for(:page_subhead) %>
  </div>
<% end %>
